// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "username",
//	      "type": "string"
//	    },
//	    {
//	      "name": "playerUUID",
//	      "type": "UUID"
//	    }
//	  ]
//	]
type LoginStart struct {
	packetID int32
	// "string"
	Username pk.String
	// "UUID"
	PlayerUUID pk.UUID
}

// NewLoginStart creates a new LoginStart packet with the correct packet ID.
func NewLoginStart() *LoginStart {
	return &LoginStart{packetID: 0}
}

// PacketID returns the protocol ID for this packet type.
func (p *LoginStart) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *LoginStart) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *LoginStart) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Username,
		&p.PlayerUUID)
}

// Scan deserializes a wire-format packet into this struct.
func (p *LoginStart) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Username,
		&p.PlayerUUID)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *LoginStart) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Username"] = &p.Username
	fields["PlayerUUID"] = &p.PlayerUUID
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *LoginStart) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Username"]; ok {
		p.Username = *val.(*pk.String)
	}
	if val, ok := fields["PlayerUUID"]; ok {
		p.PlayerUUID = *val.(*pk.UUID)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetUsername returns the Username field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *LoginStart) GetUsername() pk.String {
	return p.Username
}

// SetUsername sets the Username field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *LoginStart) SetUsername(val pk.String) {
	p.Username = val
}

// GetPlayerUUID returns the PlayerUUID field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *LoginStart) GetPlayerUUID() pk.UUID {
	return p.PlayerUUID
}

// SetPlayerUUID sets the PlayerUUID field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *LoginStart) SetPlayerUUID(val pk.UUID) {
	p.PlayerUUID = val
}

func (t *LoginStart) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Username.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Username")
	}
	bytesRead, err = t.PlayerUUID.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field PlayerUUID")
	}

	return totalBytes, nil
}

func (t LoginStart) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[LoginStart.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Username.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.PlayerUUID.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
