// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.7/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "slot",
//	      "type": "i16"
//	    },
//	    {
//	      "name": "item",
//	      "type": "UntrustedSlot"
//	    }
//	  ]
//	]
type SetCreativeSlot struct {
	packetID int32
	// "i16"
	Slot pk.Short
	// "UntrustedSlot"
	Item basetypes.UntrustedSlot
}

// NewSetCreativeSlot creates a new SetCreativeSlot packet with the correct packet ID.
func NewSetCreativeSlot() *SetCreativeSlot {
	return &SetCreativeSlot{packetID: 55}
}

// PacketID returns the protocol ID for this packet type.
func (p *SetCreativeSlot) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *SetCreativeSlot) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *SetCreativeSlot) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Slot,
		&p.Item)
}

// Scan deserializes a wire-format packet into this struct.
func (p *SetCreativeSlot) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Slot,
		&p.Item)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *SetCreativeSlot) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Slot"] = p.Slot
	fields["Item"] = p.Item
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *SetCreativeSlot) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Slot"]; ok {
		p.Slot = val.(pk.Short)
	}
	if val, ok := fields["Item"]; ok {
		p.Item = val.(basetypes.UntrustedSlot)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetSlot returns the Slot field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetCreativeSlot) GetSlot() pk.Short {
	return p.Slot
}

// SetSlot sets the Slot field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetCreativeSlot) SetSlot(val pk.Short) {
	p.Slot = val
}

// GetItem returns the Item field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetCreativeSlot) GetItem() basetypes.UntrustedSlot {
	return p.Item
}

// SetItem sets the Item field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetCreativeSlot) SetItem(val basetypes.UntrustedSlot) {
	p.Item = val
}

func (t *SetCreativeSlot) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Slot.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Slot")
	}
	bytesRead, err = t.Item.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Item")
	}

	return totalBytes, nil
}

func (t SetCreativeSlot) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[SetCreativeSlot.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Slot.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Item.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
