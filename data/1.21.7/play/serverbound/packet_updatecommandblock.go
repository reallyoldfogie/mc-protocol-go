// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.7/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "location",
//	      "type": "position"
//	    },
//	    {
//	      "name": "command",
//	      "type": "string"
//	    },
//	    {
//	      "name": "mode",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "flags",
//	      "type": "u8"
//	    }
//	  ]
//	]
type UpdateCommandBlock struct {
	packetID int32
	// "position"
	Location basetypes.Position
	// "string"
	Command pk.String
	// "varint"
	Mode pk.VarInt
	// "u8"
	Flags pk.UnsignedByte
}

// NewUpdateCommandBlock creates a new UpdateCommandBlock packet with the correct packet ID.
func NewUpdateCommandBlock() *UpdateCommandBlock {
	return &UpdateCommandBlock{packetID: 53}
}

// PacketID returns the protocol ID for this packet type.
func (p *UpdateCommandBlock) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *UpdateCommandBlock) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *UpdateCommandBlock) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Location,
		&p.Command,
		&p.Mode,
		&p.Flags)
}

// Scan deserializes a wire-format packet into this struct.
func (p *UpdateCommandBlock) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Location,
		&p.Command,
		&p.Mode,
		&p.Flags)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *UpdateCommandBlock) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Location"] = p.Location
	fields["Command"] = p.Command
	fields["Mode"] = p.Mode
	fields["Flags"] = p.Flags
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *UpdateCommandBlock) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Location"]; ok {
		p.Location = val.(basetypes.Position)
	}
	if val, ok := fields["Command"]; ok {
		p.Command = val.(pk.String)
	}
	if val, ok := fields["Mode"]; ok {
		p.Mode = val.(pk.VarInt)
	}
	if val, ok := fields["Flags"]; ok {
		p.Flags = val.(pk.UnsignedByte)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetLocation returns the Location field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) GetLocation() basetypes.Position {
	return p.Location
}

// SetLocation sets the Location field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) SetLocation(val basetypes.Position) {
	p.Location = val
}

// GetCommand returns the Command field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) GetCommand() pk.String {
	return p.Command
}

// SetCommand sets the Command field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) SetCommand(val pk.String) {
	p.Command = val
}

// GetMode returns the Mode field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) GetMode() pk.VarInt {
	return p.Mode
}

// SetMode sets the Mode field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) SetMode(val pk.VarInt) {
	p.Mode = val
}

// GetFlags returns the Flags field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) GetFlags() pk.UnsignedByte {
	return p.Flags
}

// SetFlags sets the Flags field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateCommandBlock) SetFlags(val pk.UnsignedByte) {
	p.Flags = val
}

func (t *UpdateCommandBlock) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Location.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Location")
	}
	bytesRead, err = t.Command.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Command")
	}
	bytesRead, err = t.Mode.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Mode")
	}
	bytesRead, err = t.Flags.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Flags")
	}

	return totalBytes, nil
}

func (t UpdateCommandBlock) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[UpdateCommandBlock.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Location.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Command.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Mode.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Flags.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
