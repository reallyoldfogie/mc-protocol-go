// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "slotId",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "selectedItemIndex",
//	      "type": "varint"
//	    }
//	  ]
//	]
type SelectBundleItem struct {
	packetID int32
	// "varint"
	SlotId pk.VarInt
	// "varint"
	SelectedItemIndex pk.VarInt
}

// NewSelectBundleItem creates a new SelectBundleItem packet with the correct packet ID.
func NewSelectBundleItem() *SelectBundleItem {
	return &SelectBundleItem{packetID: 3}
}

// PacketID returns the protocol ID for this packet type.
func (p *SelectBundleItem) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *SelectBundleItem) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *SelectBundleItem) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.SlotId,
		&p.SelectedItemIndex)
}

// Scan deserializes a wire-format packet into this struct.
func (p *SelectBundleItem) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.SlotId,
		&p.SelectedItemIndex)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *SelectBundleItem) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["SlotId"] = &p.SlotId
	fields["SelectedItemIndex"] = &p.SelectedItemIndex
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *SelectBundleItem) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["SlotId"]; ok {
		p.SlotId = *val.(*pk.VarInt)
	}
	if val, ok := fields["SelectedItemIndex"]; ok {
		p.SelectedItemIndex = *val.(*pk.VarInt)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetSlotId returns the SlotId field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SelectBundleItem) GetSlotId() pk.VarInt {
	return p.SlotId
}

// SetSlotId sets the SlotId field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SelectBundleItem) SetSlotId(val pk.VarInt) {
	p.SlotId = val
}

// GetSelectedItemIndex returns the SelectedItemIndex field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SelectBundleItem) GetSelectedItemIndex() pk.VarInt {
	return p.SelectedItemIndex
}

// SetSelectedItemIndex sets the SelectedItemIndex field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SelectBundleItem) SetSelectedItemIndex(val pk.VarInt) {
	p.SelectedItemIndex = val
}

func (t *SelectBundleItem) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.SlotId.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field SlotId")
	}
	bytesRead, err = t.SelectedItemIndex.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field SelectedItemIndex")
	}

	return totalBytes, nil
}

func (t SelectBundleItem) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[SelectBundleItem.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.SlotId.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.SelectedItemIndex.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
