// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "uuid",
//	      "type": "UUID"
//	    },
//	    {
//	      "name": "result",
//	      "type": "varint"
//	    }
//	  ]
//	]
type ResourcePackReceive struct {
	packetID int32
	// "UUID"
	Uuid pk.UUID
	// "varint"
	Result pk.VarInt
}

// NewResourcePackReceive creates a new ResourcePackReceive packet with the correct packet ID.
func NewResourcePackReceive() *ResourcePackReceive {
	return &ResourcePackReceive{packetID: 49}
}

// PacketID returns the protocol ID for this packet type.
func (p *ResourcePackReceive) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *ResourcePackReceive) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *ResourcePackReceive) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Uuid,
		&p.Result)
}

// Scan deserializes a wire-format packet into this struct.
func (p *ResourcePackReceive) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Uuid,
		&p.Result)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *ResourcePackReceive) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Uuid"] = &p.Uuid
	fields["Result"] = &p.Result
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *ResourcePackReceive) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Uuid"]; ok {
		p.Uuid = *val.(*pk.UUID)
	}
	if val, ok := fields["Result"]; ok {
		p.Result = *val.(*pk.VarInt)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetUuid returns the Uuid field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ResourcePackReceive) GetUuid() pk.UUID {
	return p.Uuid
}

// SetUuid sets the Uuid field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ResourcePackReceive) SetUuid(val pk.UUID) {
	p.Uuid = val
}

// GetResult returns the Result field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ResourcePackReceive) GetResult() pk.VarInt {
	return p.Result
}

// SetResult sets the Result field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ResourcePackReceive) SetResult(val pk.VarInt) {
	p.Result = val
}

func (t *ResourcePackReceive) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Uuid.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Uuid")
	}
	bytesRead, err = t.Result.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Result")
	}

	return totalBytes, nil
}

func (t ResourcePackReceive) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[ResourcePackReceive.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Uuid.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Result.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
