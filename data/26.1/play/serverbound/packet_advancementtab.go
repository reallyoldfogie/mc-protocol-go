// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"bytes"
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "action",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "tabId",
//	      "type": [
//	        "switch",
//	        {
//	          "compareTo": "action",
//	          "fields": {
//	            "0": "string",
//	            "1": "void"
//	          }
//	        }
//	      ]
//	    }
//	  ]
//	]
type AdvancementTab struct {
	packetID int32
	// "varint"
	Action pk.VarInt
	// [
	//                 "switch",
	//                 {
	//                   "compareTo": "action",
	//                   "fields": {
	//                     "0": "string",
	//                     "1": "void"
	//                   }
	//                 }
	//               ]
	TabId pk.Field
}

// NewAdvancementTab creates a new AdvancementTab packet with the correct packet ID.
func NewAdvancementTab() *AdvancementTab {
	return &AdvancementTab{packetID: 50}
}

// PacketID returns the protocol ID for this packet type.
func (p *AdvancementTab) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *AdvancementTab) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *AdvancementTab) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Action,
		p.TabId)
}

// Scan deserializes a wire-format packet into this struct.
func (p *AdvancementTab) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	// This packet has switch fields, so we read fields individually
	r := bytes.NewReader(packet.Data)
	var totalBytes int64
	var bytesRead int64
	var err error
	bytesRead, err = p.Action.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return fmt.Errorf("scanning packet field[Action] error: %w", err)
	}
	// Switch field TabId based on action
	// Convert compareTo value to string for matching
	compareValueTabId := fmt.Sprintf("%v", p.Action)

	switch compareValueTabId {
	case "0":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[TabId] case 0")
		}
		p.TabId = &val
	case "1":
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "failed to read void switch field TabId case 1")
		}
		p.TabId = &__void
	default:
		// No explicit default; treat as void (no data)
		// Per minecraft.wiki protocol docs: "If properties for parser are not specified, then this parser has no properties"
		// Using Buffer.ReadFrom() here would call io.ReadAll() and consume ALL remaining data, breaking array parsing
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "failed to read switch field TabId default void case")
		}
		p.TabId = &__void
	}

	_ = totalBytes // Unused in Scan()
	return nil
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *AdvancementTab) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Action"] = &p.Action
	fields["TabId"] = p.TabId
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *AdvancementTab) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Action"]; ok {
		p.Action = *val.(*pk.VarInt)
	}
	if val, ok := fields["TabId"]; ok {
		p.TabId = val.(pk.Field)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetAction returns the Action field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *AdvancementTab) GetAction() pk.VarInt {
	return p.Action
}

// SetAction sets the Action field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *AdvancementTab) SetAction(val pk.VarInt) {
	p.Action = val
}

// GetTabId returns the TabId field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *AdvancementTab) GetTabId() pk.Field {
	return p.TabId
}

// SetTabId sets the TabId field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *AdvancementTab) SetTabId(val pk.Field) {
	p.TabId = val
}

func (t *AdvancementTab) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Action.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Action")
	}
	// Switch field TabId based on action
	// Convert compareTo value to string for matching
	compareValueTabId := fmt.Sprintf("%v", t.Action)

	switch compareValueTabId {
	case "0":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field TabId case 0")
		}
		t.TabId = &val
	case "1":
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read void switch field TabId case 1")
		}
		t.TabId = &__void
	default:
		// No explicit default; treat as void (no data)
		// Per minecraft.wiki protocol docs: "If properties for parser are not specified, then this parser has no properties"
		// Using Buffer.ReadFrom() here would call io.ReadAll() and consume ALL remaining data, breaking array parsing
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field TabId default void case")
		}
		t.TabId = &__void
	}

	return totalBytes, nil
}

func (t AdvancementTab) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[AdvancementTab.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Action.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	// Switch field TabId based on action
	if t.TabId != nil {
		// Write switch field value if it implements WriteTo
		if writer, ok := t.TabId.(interface {
			WriteTo(io.Writer) (int64, error)
		}); ok {
			bytesWritten, err = writer.WriteTo(w)
			totalBytes += bytesWritten
			if err != nil {
				return totalBytes, err
			}
		} else {
			// Not a void case and doesn't implement WriteTo
			return totalBytes, fmt.Errorf("switch field TabId value does not implement WriteTo: %T", t.TabId)
		}
	}
	return totalBytes, nil
}
