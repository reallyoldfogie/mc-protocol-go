// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// PlayerInputInputsBitflags provides named accessors over a bitflag field.
type PlayerInputInputsBitflags struct {
	pk.UnsignedByte
}

func (bf PlayerInputInputsBitflags) Forward() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 0)) != 0
}

func (bf *PlayerInputInputsBitflags) SetForward(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 0)
	} else {
		v &^= (1 << 0)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Backward() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 1)) != 0
}

func (bf *PlayerInputInputsBitflags) SetBackward(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 1)
	} else {
		v &^= (1 << 1)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Left() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 2)) != 0
}

func (bf *PlayerInputInputsBitflags) SetLeft(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 2)
	} else {
		v &^= (1 << 2)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Right() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 3)) != 0
}

func (bf *PlayerInputInputsBitflags) SetRight(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 3)
	} else {
		v &^= (1 << 3)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Jump() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 4)) != 0
}

func (bf *PlayerInputInputsBitflags) SetJump(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 4)
	} else {
		v &^= (1 << 4)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Shift() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 5)) != 0
}

func (bf *PlayerInputInputsBitflags) SetShift(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 5)
	} else {
		v &^= (1 << 5)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}
func (bf PlayerInputInputsBitflags) Sprint() bool {
	v := uint64(uint8(bf.UnsignedByte))
	return (v & (1 << 6)) != 0
}

func (bf *PlayerInputInputsBitflags) SetSprint(value bool) {
	v := uint8(bf.UnsignedByte)
	if value {
		v |= (1 << 6)
	} else {
		v &^= (1 << 6)
	}
	bf.UnsignedByte = pk.UnsignedByte(v)
}

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "inputs",
//	      "type": [
//	        "bitflags",
//	        {
//	          "type": "u8",
//	          "flags": [
//	            "forward",
//	            "backward",
//	            "left",
//	            "right",
//	            "jump",
//	            "shift",
//	            "sprint"
//	          ]
//	        }
//	      ]
//	    }
//	  ]
//	]
type PlayerInput struct {
	packetID int32
	// [
	//                 "bitflags",
	//                 {
	//                   "type": "u8",
	//                   "flags": [
	//                     "forward",
	//                     "backward",
	//                     "left",
	//                     "right",
	//                     "jump",
	//                     "shift",
	//                     "sprint"
	//                   ]
	//                 }
	//               ]
	Inputs PlayerInputInputsBitflags
}

// NewPlayerInput creates a new PlayerInput packet with the correct packet ID.
func NewPlayerInput() *PlayerInput {
	return &PlayerInput{packetID: 40}
}

// PacketID returns the protocol ID for this packet type.
func (p *PlayerInput) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *PlayerInput) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *PlayerInput) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Inputs)
}

// Scan deserializes a wire-format packet into this struct.
func (p *PlayerInput) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Inputs)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *PlayerInput) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Inputs"] = &p.Inputs
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *PlayerInput) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Inputs"]; ok {
		p.Inputs = *val.(*PlayerInputInputsBitflags)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetInputs returns the Inputs field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *PlayerInput) GetInputs() PlayerInputInputsBitflags {
	return p.Inputs
}

// SetInputs sets the Inputs field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *PlayerInput) SetInputs(val PlayerInputInputsBitflags) {
	p.Inputs = val
}

func (t *PlayerInput) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Inputs.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Inputs")
	}

	return totalBytes, nil
}

func (t PlayerInput) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[PlayerInput.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Inputs.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
