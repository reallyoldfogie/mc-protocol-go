// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "hand",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "pages",
//	      "type": [
//	        "array",
//	        {
//	          "countType": "varint",
//	          "type": "string"
//	        }
//	      ]
//	    },
//	    {
//	      "name": "title",
//	      "type": [
//	        "option",
//	        "string"
//	      ]
//	    }
//	  ]
//	]
type EditBook struct {
	packetID int32
	// "varint"
	Hand pk.VarInt
	// [
	//                 "array",
	//                 {
	//                   "countType": "varint",
	//                   "type": "string"
	//                 }
	//               ]
	Pages models.Array[pk.VarInt, pk.String]
	// [
	//                 "option",
	//                 "string"
	//               ]
	Title models.Option[pk.String]
}

// NewEditBook creates a new EditBook packet with the correct packet ID.
func NewEditBook() *EditBook {
	return &EditBook{packetID: 23}
}

// PacketID returns the protocol ID for this packet type.
func (p *EditBook) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *EditBook) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *EditBook) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Hand,
		&p.Pages,
		&p.Title)
}

// Scan deserializes a wire-format packet into this struct.
func (p *EditBook) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Hand,
		&p.Pages,
		&p.Title)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *EditBook) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Hand"] = &p.Hand
	fields["Pages"] = &p.Pages
	fields["Title"] = &p.Title
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *EditBook) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Hand"]; ok {
		p.Hand = *val.(*pk.VarInt)
	}
	if val, ok := fields["Pages"]; ok {
		p.Pages = *val.(*models.Array[pk.VarInt, pk.String])
	}
	if val, ok := fields["Title"]; ok {
		p.Title = *val.(*models.Option[pk.String])
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetHand returns the Hand field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *EditBook) GetHand() pk.VarInt {
	return p.Hand
}

// SetHand sets the Hand field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *EditBook) SetHand(val pk.VarInt) {
	p.Hand = val
}

// GetPages returns the Pages field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *EditBook) GetPages() models.Array[pk.VarInt, pk.String] {
	return p.Pages
}

// SetPages sets the Pages field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *EditBook) SetPages(val models.Array[pk.VarInt, pk.String]) {
	p.Pages = val
}

// GetTitle returns the Title field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *EditBook) GetTitle() models.Option[pk.String] {
	return p.Title
}

// SetTitle sets the Title field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *EditBook) SetTitle(val models.Option[pk.String]) {
	p.Title = val
}

func (t *EditBook) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Hand.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Hand")
	}
	bytesRead, err = t.Pages.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Pages")
	}
	bytesRead, err = t.Title.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Title")
	}

	return totalBytes, nil
}

func (t EditBook) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[EditBook.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Hand.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Pages.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Title.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
