// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "tick_rate",
//	      "type": "f32"
//	    },
//	    {
//	      "name": "is_frozen",
//	      "type": "bool"
//	    }
//	  ]
//	]
type SetTickingState struct {
	packetID int32
	// "f32"
	TickRate pk.Float
	// "bool"
	IsFrozen pk.Boolean
}

// NewSetTickingState creates a new SetTickingState packet with the correct packet ID.
func NewSetTickingState() *SetTickingState {
	return &SetTickingState{packetID: 113}
}

// PacketID returns the protocol ID for this packet type.
func (p *SetTickingState) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *SetTickingState) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *SetTickingState) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.TickRate,
		&p.IsFrozen)
}

// Scan deserializes a wire-format packet into this struct.
func (p *SetTickingState) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.TickRate,
		&p.IsFrozen)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *SetTickingState) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["TickRate"] = &p.TickRate
	fields["IsFrozen"] = &p.IsFrozen
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *SetTickingState) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["TickRate"]; ok {
		p.TickRate = *val.(*pk.Float)
	}
	if val, ok := fields["IsFrozen"]; ok {
		p.IsFrozen = *val.(*pk.Boolean)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetTickRate returns the TickRate field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetTickingState) GetTickRate() pk.Float {
	return p.TickRate
}

// SetTickRate sets the TickRate field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetTickingState) SetTickRate(val pk.Float) {
	p.TickRate = val
}

// GetIsFrozen returns the IsFrozen field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetTickingState) GetIsFrozen() pk.Boolean {
	return p.IsFrozen
}

// SetIsFrozen sets the IsFrozen field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetTickingState) SetIsFrozen(val pk.Boolean) {
	p.IsFrozen = val
}

func (t *SetTickingState) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.TickRate.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field TickRate")
	}
	bytesRead, err = t.IsFrozen.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field IsFrozen")
	}

	return totalBytes, nil
}

func (t SetTickingState) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[SetTickingState.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.TickRate.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.IsFrozen.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
