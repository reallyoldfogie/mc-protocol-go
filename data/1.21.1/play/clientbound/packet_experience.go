// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "experienceBar",
//	      "type": "f32"
//	    },
//	    {
//	      "name": "level",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "totalExperience",
//	      "type": "varint"
//	    }
//	  ]
//	]
type Experience struct {
	packetID int32
	// "f32"
	ExperienceBar pk.Float
	// "varint"
	Level pk.VarInt
	// "varint"
	TotalExperience pk.VarInt
}

// NewExperience creates a new Experience packet with the correct packet ID.
func NewExperience() *Experience {
	return &Experience{packetID: 92}
}

// PacketID returns the protocol ID for this packet type.
func (p *Experience) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *Experience) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *Experience) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.ExperienceBar,
		&p.Level,
		&p.TotalExperience)
}

// Scan deserializes a wire-format packet into this struct.
func (p *Experience) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.ExperienceBar,
		&p.Level,
		&p.TotalExperience)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *Experience) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["ExperienceBar"] = p.ExperienceBar
	fields["Level"] = p.Level
	fields["TotalExperience"] = p.TotalExperience
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *Experience) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["ExperienceBar"]; ok {
		p.ExperienceBar = val.(pk.Float)
	}
	if val, ok := fields["Level"]; ok {
		p.Level = val.(pk.VarInt)
	}
	if val, ok := fields["TotalExperience"]; ok {
		p.TotalExperience = val.(pk.VarInt)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetExperienceBar returns the ExperienceBar field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Experience) GetExperienceBar() pk.Float {
	return p.ExperienceBar
}

// SetExperienceBar sets the ExperienceBar field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Experience) SetExperienceBar(val pk.Float) {
	p.ExperienceBar = val
}

// GetLevel returns the Level field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Experience) GetLevel() pk.VarInt {
	return p.Level
}

// SetLevel sets the Level field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Experience) SetLevel(val pk.VarInt) {
	p.Level = val
}

// GetTotalExperience returns the TotalExperience field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Experience) GetTotalExperience() pk.VarInt {
	return p.TotalExperience
}

// SetTotalExperience sets the TotalExperience field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Experience) SetTotalExperience(val pk.VarInt) {
	p.TotalExperience = val
}

func (t *Experience) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.ExperienceBar.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field ExperienceBar")
	}
	bytesRead, err = t.Level.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Level")
	}
	bytesRead, err = t.TotalExperience.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field TotalExperience")
	}

	return totalBytes, nil
}

func (t Experience) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[Experience.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.ExperienceBar.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Level.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.TotalExperience.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
