// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "sessionUUID",
//	      "type": "UUID"
//	    },
//	    {
//	      "name": "expireTime",
//	      "type": "i64"
//	    },
//	    {
//	      "name": "publicKey",
//	      "type": "ByteArray"
//	    },
//	    {
//	      "name": "signature",
//	      "type": "ByteArray"
//	    }
//	  ]
//	]
type ChatSessionUpdate struct {
	packetID int32
	// "UUID"
	SessionUUID pk.UUID
	// "i64"
	ExpireTime pk.Long
	// "ByteArray"
	PublicKey pk.ByteArray
	// "ByteArray"
	Signature pk.ByteArray
}

// NewChatSessionUpdate creates a new ChatSessionUpdate packet with the correct packet ID.
func NewChatSessionUpdate() *ChatSessionUpdate {
	return &ChatSessionUpdate{packetID: 7}
}

// PacketID returns the protocol ID for this packet type.
func (p *ChatSessionUpdate) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *ChatSessionUpdate) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *ChatSessionUpdate) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.SessionUUID,
		&p.ExpireTime,
		&p.PublicKey,
		&p.Signature)
}

// Scan deserializes a wire-format packet into this struct.
func (p *ChatSessionUpdate) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.SessionUUID,
		&p.ExpireTime,
		&p.PublicKey,
		&p.Signature)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *ChatSessionUpdate) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["SessionUUID"] = &p.SessionUUID
	fields["ExpireTime"] = &p.ExpireTime
	fields["PublicKey"] = &p.PublicKey
	fields["Signature"] = &p.Signature
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *ChatSessionUpdate) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["SessionUUID"]; ok {
		p.SessionUUID = *val.(*pk.UUID)
	}
	if val, ok := fields["ExpireTime"]; ok {
		p.ExpireTime = *val.(*pk.Long)
	}
	if val, ok := fields["PublicKey"]; ok {
		p.PublicKey = *val.(*pk.ByteArray)
	}
	if val, ok := fields["Signature"]; ok {
		p.Signature = *val.(*pk.ByteArray)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetSessionUUID returns the SessionUUID field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) GetSessionUUID() pk.UUID {
	return p.SessionUUID
}

// SetSessionUUID sets the SessionUUID field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) SetSessionUUID(val pk.UUID) {
	p.SessionUUID = val
}

// GetExpireTime returns the ExpireTime field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) GetExpireTime() pk.Long {
	return p.ExpireTime
}

// SetExpireTime sets the ExpireTime field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) SetExpireTime(val pk.Long) {
	p.ExpireTime = val
}

// GetPublicKey returns the PublicKey field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) GetPublicKey() pk.ByteArray {
	return p.PublicKey
}

// SetPublicKey sets the PublicKey field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) SetPublicKey(val pk.ByteArray) {
	p.PublicKey = val
}

// GetSignature returns the Signature field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) GetSignature() pk.ByteArray {
	return p.Signature
}

// SetSignature sets the Signature field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSessionUpdate) SetSignature(val pk.ByteArray) {
	p.Signature = val
}

func (t *ChatSessionUpdate) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.SessionUUID.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field SessionUUID")
	}
	bytesRead, err = t.ExpireTime.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field ExpireTime")
	}
	bytesRead, err = t.PublicKey.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field PublicKey")
	}
	bytesRead, err = t.Signature.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Signature")
	}

	return totalBytes, nil
}

func (t ChatSessionUpdate) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[ChatSessionUpdate.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.SessionUUID.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.ExpireTime.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.PublicKey.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Signature.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
