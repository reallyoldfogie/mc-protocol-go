// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.4/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "windowId",
//	      "type": "ContainerID"
//	    },
//	    {
//	      "name": "nbSlots",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "entityId",
//	      "type": "i32"
//	    }
//	  ]
//	]
type OpenHorseWindow struct {
	packetID int32
	// "ContainerID"
	WindowId basetypes.ContainerID
	// "varint"
	NbSlots pk.VarInt
	// "i32"
	EntityId pk.Int
}

// NewOpenHorseWindow creates a new OpenHorseWindow packet with the correct packet ID.
func NewOpenHorseWindow() *OpenHorseWindow {
	return &OpenHorseWindow{packetID: 36}
}

// PacketID returns the protocol ID for this packet type.
func (p *OpenHorseWindow) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *OpenHorseWindow) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *OpenHorseWindow) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.WindowId,
		&p.NbSlots,
		&p.EntityId)
}

// Scan deserializes a wire-format packet into this struct.
func (p *OpenHorseWindow) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.WindowId,
		&p.NbSlots,
		&p.EntityId)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *OpenHorseWindow) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["WindowId"] = p.WindowId
	fields["NbSlots"] = p.NbSlots
	fields["EntityId"] = p.EntityId
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *OpenHorseWindow) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["WindowId"]; ok {
		p.WindowId = val.(basetypes.ContainerID)
	}
	if val, ok := fields["NbSlots"]; ok {
		p.NbSlots = val.(pk.VarInt)
	}
	if val, ok := fields["EntityId"]; ok {
		p.EntityId = val.(pk.Int)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetWindowId returns the WindowId field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *OpenHorseWindow) GetWindowId() basetypes.ContainerID {
	return p.WindowId
}

// SetWindowId sets the WindowId field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *OpenHorseWindow) SetWindowId(val basetypes.ContainerID) {
	p.WindowId = val
}

// GetNbSlots returns the NbSlots field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *OpenHorseWindow) GetNbSlots() pk.VarInt {
	return p.NbSlots
}

// SetNbSlots sets the NbSlots field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *OpenHorseWindow) SetNbSlots(val pk.VarInt) {
	p.NbSlots = val
}

// GetEntityId returns the EntityId field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *OpenHorseWindow) GetEntityId() pk.Int {
	return p.EntityId
}

// SetEntityId sets the EntityId field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *OpenHorseWindow) SetEntityId(val pk.Int) {
	p.EntityId = val
}

func (t *OpenHorseWindow) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.WindowId.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field WindowId")
	}
	bytesRead, err = t.NbSlots.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field NbSlots")
	}
	bytesRead, err = t.EntityId.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field EntityId")
	}

	return totalBytes, nil
}

func (t OpenHorseWindow) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[OpenHorseWindow.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.WindowId.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.NbSlots.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.EntityId.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
