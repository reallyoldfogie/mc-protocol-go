// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.10/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "position",
//	      "type": "position"
//	    },
//	    {
//	      "name": "mode",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "message",
//	      "type": "string"
//	    }
//	  ]
//	]
type SetTestBlock struct {
	packetID int32
	// "position"
	Position basetypes.Position
	// "varint"
	Mode pk.VarInt
	// "string"
	Message pk.String
}

// NewSetTestBlock creates a new SetTestBlock packet with the correct packet ID.
func NewSetTestBlock() *SetTestBlock {
	return &SetTestBlock{packetID: 58}
}

// PacketID returns the protocol ID for this packet type.
func (p *SetTestBlock) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *SetTestBlock) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *SetTestBlock) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Position,
		&p.Mode,
		&p.Message)
}

// Scan deserializes a wire-format packet into this struct.
func (p *SetTestBlock) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Position,
		&p.Mode,
		&p.Message)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *SetTestBlock) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Position"] = p.Position
	fields["Mode"] = p.Mode
	fields["Message"] = p.Message
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *SetTestBlock) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Position"]; ok {
		p.Position = val.(basetypes.Position)
	}
	if val, ok := fields["Mode"]; ok {
		p.Mode = val.(pk.VarInt)
	}
	if val, ok := fields["Message"]; ok {
		p.Message = val.(pk.String)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetPosition returns the Position field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetTestBlock) GetPosition() basetypes.Position {
	return p.Position
}

// SetPosition sets the Position field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetTestBlock) SetPosition(val basetypes.Position) {
	p.Position = val
}

// GetMode returns the Mode field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetTestBlock) GetMode() pk.VarInt {
	return p.Mode
}

// SetMode sets the Mode field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetTestBlock) SetMode(val pk.VarInt) {
	p.Mode = val
}

// GetMessage returns the Message field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *SetTestBlock) GetMessage() pk.String {
	return p.Message
}

// SetMessage sets the Message field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *SetTestBlock) SetMessage(val pk.String) {
	p.Message = val
}

func (t *SetTestBlock) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Position.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Position")
	}
	bytesRead, err = t.Mode.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Mode")
	}
	bytesRead, err = t.Message.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Message")
	}

	return totalBytes, nil
}

func (t SetTestBlock) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[SetTestBlock.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Position.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Mode.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Message.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
