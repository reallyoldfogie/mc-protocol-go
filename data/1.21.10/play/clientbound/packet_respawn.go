// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "worldState",
//	      "type": "SpawnInfo"
//	    },
//	    {
//	      "name": "copyMetadata",
//	      "type": "u8"
//	    }
//	  ]
//	]
type Respawn struct {
	packetID int32
	// "SpawnInfo"
	WorldState SpawnInfo
	// "u8"
	CopyMetadata pk.UnsignedByte
}

// NewRespawn creates a new Respawn packet with the correct packet ID.
func NewRespawn() *Respawn {
	return &Respawn{packetID: 80}
}

// PacketID returns the protocol ID for this packet type.
func (p *Respawn) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *Respawn) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *Respawn) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.WorldState,
		&p.CopyMetadata)
}

// Scan deserializes a wire-format packet into this struct.
func (p *Respawn) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.WorldState,
		&p.CopyMetadata)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *Respawn) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["WorldState"] = &p.WorldState
	fields["CopyMetadata"] = &p.CopyMetadata
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *Respawn) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["WorldState"]; ok {
		p.WorldState = *val.(*SpawnInfo)
	}
	if val, ok := fields["CopyMetadata"]; ok {
		p.CopyMetadata = *val.(*pk.UnsignedByte)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetWorldState returns the WorldState field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Respawn) GetWorldState() SpawnInfo {
	return p.WorldState
}

// SetWorldState sets the WorldState field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Respawn) SetWorldState(val SpawnInfo) {
	p.WorldState = val
}

// GetCopyMetadata returns the CopyMetadata field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Respawn) GetCopyMetadata() pk.UnsignedByte {
	return p.CopyMetadata
}

// SetCopyMetadata sets the CopyMetadata field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Respawn) SetCopyMetadata(val pk.UnsignedByte) {
	p.CopyMetadata = val
}

func (t *Respawn) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.WorldState.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field WorldState")
	}
	bytesRead, err = t.CopyMetadata.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field CopyMetadata")
	}

	return totalBytes, nil
}

func (t Respawn) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[Respawn.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.WorldState.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.CopyMetadata.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
