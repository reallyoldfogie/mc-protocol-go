// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "yaw",
//	      "type": "f32"
//	    },
//	    {
//	      "name": "relativeYaw",
//	      "type": "bool"
//	    },
//	    {
//	      "name": "pitch",
//	      "type": "f32"
//	    },
//	    {
//	      "name": "relativePitch",
//	      "type": "bool"
//	    }
//	  ]
//	]
type PlayerRotation struct {
	packetID int32
	// "f32"
	Yaw pk.Float
	// "bool"
	RelativeYaw pk.Boolean
	// "f32"
	Pitch pk.Float
	// "bool"
	RelativePitch pk.Boolean
}

// NewPlayerRotation creates a new PlayerRotation packet with the correct packet ID.
func NewPlayerRotation() *PlayerRotation {
	return &PlayerRotation{packetID: 71}
}

// PacketID returns the protocol ID for this packet type.
func (p *PlayerRotation) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *PlayerRotation) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *PlayerRotation) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Yaw,
		&p.RelativeYaw,
		&p.Pitch,
		&p.RelativePitch)
}

// Scan deserializes a wire-format packet into this struct.
func (p *PlayerRotation) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Yaw,
		&p.RelativeYaw,
		&p.Pitch,
		&p.RelativePitch)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *PlayerRotation) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Yaw"] = &p.Yaw
	fields["RelativeYaw"] = &p.RelativeYaw
	fields["Pitch"] = &p.Pitch
	fields["RelativePitch"] = &p.RelativePitch
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *PlayerRotation) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Yaw"]; ok {
		p.Yaw = *val.(*pk.Float)
	}
	if val, ok := fields["RelativeYaw"]; ok {
		p.RelativeYaw = *val.(*pk.Boolean)
	}
	if val, ok := fields["Pitch"]; ok {
		p.Pitch = *val.(*pk.Float)
	}
	if val, ok := fields["RelativePitch"]; ok {
		p.RelativePitch = *val.(*pk.Boolean)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetYaw returns the Yaw field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *PlayerRotation) GetYaw() pk.Float {
	return p.Yaw
}

// SetYaw sets the Yaw field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *PlayerRotation) SetYaw(val pk.Float) {
	p.Yaw = val
}

// GetRelativeYaw returns the RelativeYaw field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *PlayerRotation) GetRelativeYaw() pk.Boolean {
	return p.RelativeYaw
}

// SetRelativeYaw sets the RelativeYaw field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *PlayerRotation) SetRelativeYaw(val pk.Boolean) {
	p.RelativeYaw = val
}

// GetPitch returns the Pitch field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *PlayerRotation) GetPitch() pk.Float {
	return p.Pitch
}

// SetPitch sets the Pitch field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *PlayerRotation) SetPitch(val pk.Float) {
	p.Pitch = val
}

// GetRelativePitch returns the RelativePitch field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *PlayerRotation) GetRelativePitch() pk.Boolean {
	return p.RelativePitch
}

// SetRelativePitch sets the RelativePitch field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *PlayerRotation) SetRelativePitch(val pk.Boolean) {
	p.RelativePitch = val
}

func (t *PlayerRotation) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Yaw.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Yaw")
	}
	bytesRead, err = t.RelativeYaw.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field RelativeYaw")
	}
	bytesRead, err = t.Pitch.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Pitch")
	}
	bytesRead, err = t.RelativePitch.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field RelativePitch")
	}

	return totalBytes, nil
}

func (t PlayerRotation) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[PlayerRotation.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Yaw.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.RelativeYaw.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Pitch.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.RelativePitch.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
