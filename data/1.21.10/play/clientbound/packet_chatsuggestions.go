// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "action",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "entries",
//	      "type": [
//	        "array",
//	        {
//	          "countType": "varint",
//	          "type": "string"
//	        }
//	      ]
//	    }
//	  ]
//	]
type ChatSuggestions struct {
	packetID int32
	// "varint"
	Action pk.VarInt
	// [
	//                 "array",
	//                 {
	//                   "countType": "varint",
	//                   "type": "string"
	//                 }
	//               ]
	Entries models.Array[pk.VarInt, pk.String]
}

// NewChatSuggestions creates a new ChatSuggestions packet with the correct packet ID.
func NewChatSuggestions() *ChatSuggestions {
	return &ChatSuggestions{packetID: 23}
}

// PacketID returns the protocol ID for this packet type.
func (p *ChatSuggestions) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *ChatSuggestions) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *ChatSuggestions) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Action,
		&p.Entries)
}

// Scan deserializes a wire-format packet into this struct.
func (p *ChatSuggestions) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Action,
		&p.Entries)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *ChatSuggestions) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Action"] = &p.Action
	fields["Entries"] = &p.Entries
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *ChatSuggestions) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Action"]; ok {
		p.Action = *val.(*pk.VarInt)
	}
	if val, ok := fields["Entries"]; ok {
		p.Entries = *val.(*models.Array[pk.VarInt, pk.String])
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetAction returns the Action field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSuggestions) GetAction() pk.VarInt {
	return p.Action
}

// SetAction sets the Action field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSuggestions) SetAction(val pk.VarInt) {
	p.Action = val
}

// GetEntries returns the Entries field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *ChatSuggestions) GetEntries() models.Array[pk.VarInt, pk.String] {
	return p.Entries
}

// SetEntries sets the Entries field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *ChatSuggestions) SetEntries(val models.Array[pk.VarInt, pk.String]) {
	p.Entries = val
}

func (t *ChatSuggestions) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Action.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Action")
	}
	bytesRead, err = t.Entries.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Entries")
	}

	return totalBytes, nil
}

func (t ChatSuggestions) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[ChatSuggestions.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Action.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Entries.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
