// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "entityId",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "velocity",
//	      "type": "lpVec3"
//	    }
//	  ]
//	]
type EntityVelocity struct {
	packetID int32
	// "varint"
	EntityId pk.VarInt
	// "lpVec3"
	Velocity models.LpVec3
}

// NewEntityVelocity creates a new EntityVelocity packet with the correct packet ID.
func NewEntityVelocity() *EntityVelocity {
	return &EntityVelocity{packetID: 99}
}

// PacketID returns the protocol ID for this packet type.
func (p *EntityVelocity) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *EntityVelocity) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *EntityVelocity) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.EntityId,
		&p.Velocity)
}

// Scan deserializes a wire-format packet into this struct.
func (p *EntityVelocity) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.EntityId,
		&p.Velocity)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *EntityVelocity) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["EntityId"] = &p.EntityId
	fields["Velocity"] = &p.Velocity
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *EntityVelocity) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["EntityId"]; ok {
		p.EntityId = *val.(*pk.VarInt)
	}
	if val, ok := fields["Velocity"]; ok {
		p.Velocity = *val.(*models.LpVec3)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetEntityId returns the EntityId field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *EntityVelocity) GetEntityId() pk.VarInt {
	return p.EntityId
}

// SetEntityId sets the EntityId field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *EntityVelocity) SetEntityId(val pk.VarInt) {
	p.EntityId = val
}

// GetVelocity returns the Velocity field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *EntityVelocity) GetVelocity() models.LpVec3 {
	return p.Velocity
}

// SetVelocity sets the Velocity field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *EntityVelocity) SetVelocity(val models.LpVec3) {
	p.Velocity = val
}

func (t *EntityVelocity) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.EntityId.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field EntityId")
	}
	bytesRead, err = t.Velocity.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Velocity")
	}

	return totalBytes, nil
}

func (t EntityVelocity) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[EntityVelocity.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.EntityId.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Velocity.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
