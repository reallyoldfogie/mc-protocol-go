// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.10/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "absolutePos",
//	      "type": "position"
//	    },
//	    {
//	      "name": "relativePos",
//	      "type": "position"
//	    }
//	  ]
//	]
type GameTestHighlightPos struct {
	packetID int32
	// "position"
	AbsolutePos basetypes.Position
	// "position"
	RelativePos basetypes.Position
}

// NewGameTestHighlightPos creates a new GameTestHighlightPos packet with the correct packet ID.
func NewGameTestHighlightPos() *GameTestHighlightPos {
	return &GameTestHighlightPos{packetID: 39}
}

// PacketID returns the protocol ID for this packet type.
func (p *GameTestHighlightPos) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *GameTestHighlightPos) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *GameTestHighlightPos) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.AbsolutePos,
		&p.RelativePos)
}

// Scan deserializes a wire-format packet into this struct.
func (p *GameTestHighlightPos) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.AbsolutePos,
		&p.RelativePos)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *GameTestHighlightPos) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["AbsolutePos"] = &p.AbsolutePos
	fields["RelativePos"] = &p.RelativePos
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *GameTestHighlightPos) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["AbsolutePos"]; ok {
		p.AbsolutePos = *val.(*basetypes.Position)
	}
	if val, ok := fields["RelativePos"]; ok {
		p.RelativePos = *val.(*basetypes.Position)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetAbsolutePos returns the AbsolutePos field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *GameTestHighlightPos) GetAbsolutePos() basetypes.Position {
	return p.AbsolutePos
}

// SetAbsolutePos sets the AbsolutePos field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *GameTestHighlightPos) SetAbsolutePos(val basetypes.Position) {
	p.AbsolutePos = val
}

// GetRelativePos returns the RelativePos field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *GameTestHighlightPos) GetRelativePos() basetypes.Position {
	return p.RelativePos
}

// SetRelativePos sets the RelativePos field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *GameTestHighlightPos) SetRelativePos(val basetypes.Position) {
	p.RelativePos = val
}

func (t *GameTestHighlightPos) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.AbsolutePos.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field AbsolutePos")
	}
	bytesRead, err = t.RelativePos.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field RelativePos")
	}

	return totalBytes, nil
}

func (t GameTestHighlightPos) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[GameTestHighlightPos.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.AbsolutePos.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.RelativePos.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
