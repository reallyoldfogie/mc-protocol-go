// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "age",
//	      "type": "i64"
//	    },
//	    {
//	      "name": "time",
//	      "type": "i64"
//	    },
//	    {
//	      "name": "tickDayTime",
//	      "type": "bool"
//	    }
//	  ]
//	]
type UpdateTime struct {
	packetID int32
	// "i64"
	Age pk.Long
	// "i64"
	Time pk.Long
	// "bool"
	TickDayTime pk.Boolean
}

// NewUpdateTime creates a new UpdateTime packet with the correct packet ID.
func NewUpdateTime() *UpdateTime {
	return &UpdateTime{packetID: 106}
}

// PacketID returns the protocol ID for this packet type.
func (p *UpdateTime) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *UpdateTime) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *UpdateTime) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Age,
		&p.Time,
		&p.TickDayTime)
}

// Scan deserializes a wire-format packet into this struct.
func (p *UpdateTime) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Age,
		&p.Time,
		&p.TickDayTime)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *UpdateTime) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Age"] = p.Age
	fields["Time"] = p.Time
	fields["TickDayTime"] = p.TickDayTime
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *UpdateTime) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Age"]; ok {
		p.Age = val.(pk.Long)
	}
	if val, ok := fields["Time"]; ok {
		p.Time = val.(pk.Long)
	}
	if val, ok := fields["TickDayTime"]; ok {
		p.TickDayTime = val.(pk.Boolean)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetAge returns the Age field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateTime) GetAge() pk.Long {
	return p.Age
}

// SetAge sets the Age field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateTime) SetAge(val pk.Long) {
	p.Age = val
}

// GetTime returns the Time field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateTime) GetTime() pk.Long {
	return p.Time
}

// SetTime sets the Time field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateTime) SetTime(val pk.Long) {
	p.Time = val
}

// GetTickDayTime returns the TickDayTime field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateTime) GetTickDayTime() pk.Boolean {
	return p.TickDayTime
}

// SetTickDayTime sets the TickDayTime field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateTime) SetTickDayTime(val pk.Boolean) {
	p.TickDayTime = val
}

func (t *UpdateTime) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Age.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Age")
	}
	bytesRead, err = t.Time.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Time")
	}
	bytesRead, err = t.TickDayTime.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field TickDayTime")
	}

	return totalBytes, nil
}

func (t UpdateTime) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[UpdateTime.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Age.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Time.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.TickDayTime.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
