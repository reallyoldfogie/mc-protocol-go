// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"bytes"
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "flags",
//	      "type": "i8"
//	    },
//	    {
//	      "name": "source",
//	      "type": [
//	        "switch",
//	        {
//	          "compareTo": "flags",
//	          "fields": {
//	            "1": "varint",
//	            "3": "varint"
//	          },
//	          "default": "void"
//	        }
//	      ]
//	    },
//	    {
//	      "name": "sound",
//	      "type": [
//	        "switch",
//	        {
//	          "compareTo": "flags",
//	          "fields": {
//	            "2": "string",
//	            "3": "string"
//	          },
//	          "default": "void"
//	        }
//	      ]
//	    }
//	  ]
//	]
type StopSound struct {
	packetID int32
	// "i8"
	Flags pk.Byte
	// [
	//                 "switch",
	//                 {
	//                   "compareTo": "flags",
	//                   "fields": {
	//                     "1": "varint",
	//                     "3": "varint"
	//                   },
	//                   "default": "void"
	//                 }
	//               ]
	Source pk.Field
	// [
	//                 "switch",
	//                 {
	//                   "compareTo": "flags",
	//                   "fields": {
	//                     "2": "string",
	//                     "3": "string"
	//                   },
	//                   "default": "void"
	//                 }
	//               ]
	Sound pk.Field
}

// NewStopSound creates a new StopSound packet with the correct packet ID.
func NewStopSound() *StopSound {
	return &StopSound{packetID: 112}
}

// PacketID returns the protocol ID for this packet type.
func (p *StopSound) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *StopSound) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *StopSound) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Flags,
		p.Source,
		p.Sound)
}

// Scan deserializes a wire-format packet into this struct.
func (p *StopSound) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	// This packet has switch fields, so we read fields individually
	r := bytes.NewReader(packet.Data)
	var totalBytes int64
	var bytesRead int64
	var err error
	bytesRead, err = p.Flags.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return fmt.Errorf("scanning packet field[Flags] error: %w", err)
	}
	// Switch field Source based on flags
	// Convert compareTo value to string for matching
	compareValueSource := fmt.Sprintf("%v", p.Flags)

	switch compareValueSource {
	case "1":
		var val pk.VarInt
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[Source] case 1")
		}
		p.Source = &val
	case "3":
		var val pk.VarInt
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[Source] case 3")
		}
		p.Source = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "failed to read void switch field Source default case")
		}
		p.Source = &__void
	}

	// Switch field Sound based on flags
	// Convert compareTo value to string for matching
	compareValueSound := fmt.Sprintf("%v", p.Flags)

	switch compareValueSound {
	case "2":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[Sound] case 2")
		}
		p.Sound = &val
	case "3":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[Sound] case 3")
		}
		p.Sound = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "failed to read void switch field Sound default case")
		}
		p.Sound = &__void
	}

	_ = totalBytes // Unused in Scan()
	return nil
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *StopSound) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Flags"] = &p.Flags
	fields["Source"] = p.Source
	fields["Sound"] = p.Sound
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *StopSound) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Flags"]; ok {
		p.Flags = *val.(*pk.Byte)
	}
	if val, ok := fields["Source"]; ok {
		p.Source = val.(pk.Field)
	}
	if val, ok := fields["Sound"]; ok {
		p.Sound = val.(pk.Field)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetFlags returns the Flags field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *StopSound) GetFlags() pk.Byte {
	return p.Flags
}

// SetFlags sets the Flags field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *StopSound) SetFlags(val pk.Byte) {
	p.Flags = val
}

// GetSource returns the Source field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *StopSound) GetSource() pk.Field {
	return p.Source
}

// SetSource sets the Source field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *StopSound) SetSource(val pk.Field) {
	p.Source = val
}

// GetSound returns the Sound field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *StopSound) GetSound() pk.Field {
	return p.Sound
}

// SetSound sets the Sound field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *StopSound) SetSound(val pk.Field) {
	p.Sound = val
}

func (t *StopSound) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Flags.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Flags")
	}
	// Switch field Source based on flags
	// Convert compareTo value to string for matching
	compareValueSource := fmt.Sprintf("%v", t.Flags)

	switch compareValueSource {
	case "1":
		var val pk.VarInt
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field Source case 1")
		}
		t.Source = &val
	case "3":
		var val pk.VarInt
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field Source case 3")
		}
		t.Source = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read void switch field Source default case")
		}
		t.Source = &__void
	}

	// Switch field Sound based on flags
	// Convert compareTo value to string for matching
	compareValueSound := fmt.Sprintf("%v", t.Flags)

	switch compareValueSound {
	case "2":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field Sound case 2")
		}
		t.Sound = &val
	case "3":
		var val pk.String
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field Sound case 3")
		}
		t.Sound = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read void switch field Sound default case")
		}
		t.Sound = &__void
	}

	return totalBytes, nil
}

func (t StopSound) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[StopSound.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Flags.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	// Switch field Source based on flags
	if t.Source != nil {
		// Write switch field value if it implements WriteTo
		if writer, ok := t.Source.(interface {
			WriteTo(io.Writer) (int64, error)
		}); ok {
			bytesWritten, err = writer.WriteTo(w)
			totalBytes += bytesWritten
			if err != nil {
				return totalBytes, err
			}
		} else {
			// Not a void case and doesn't implement WriteTo
			return totalBytes, fmt.Errorf("switch field Source value does not implement WriteTo: %T", t.Source)
		}
	}
	// Switch field Sound based on flags
	if t.Sound != nil {
		// Write switch field value if it implements WriteTo
		if writer, ok := t.Sound.(interface {
			WriteTo(io.Writer) (int64, error)
		}); ok {
			bytesWritten, err = writer.WriteTo(w)
			totalBytes += bytesWritten
			if err != nil {
				return totalBytes, err
			}
		} else {
			// Not a void case and doesn't implement WriteTo
			return totalBytes, fmt.Errorf("switch field Sound value does not implement WriteTo: %T", t.Sound)
		}
	}
	return totalBytes, nil
}
