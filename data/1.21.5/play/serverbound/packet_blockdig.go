// Generated by gen_protocol. DO NOT EDIT
package serverbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/data/1.21.5/basetypes"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "status",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "location",
//	      "type": "position"
//	    },
//	    {
//	      "name": "face",
//	      "type": "i8"
//	    },
//	    {
//	      "name": "sequence",
//	      "type": "varint"
//	    }
//	  ]
//	]
type BlockDig struct {
	packetID int32
	// "varint"
	Status pk.VarInt
	// "position"
	Location basetypes.Position
	// "i8"
	Face pk.Byte
	// "varint"
	Sequence pk.VarInt
}

// NewBlockDig creates a new BlockDig packet with the correct packet ID.
func NewBlockDig() *BlockDig {
	return &BlockDig{packetID: 39}
}

// PacketID returns the protocol ID for this packet type.
func (p *BlockDig) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *BlockDig) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *BlockDig) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Status,
		&p.Location,
		&p.Face,
		&p.Sequence)
}

// Scan deserializes a wire-format packet into this struct.
func (p *BlockDig) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Status,
		&p.Location,
		&p.Face,
		&p.Sequence)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *BlockDig) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Status"] = p.Status
	fields["Location"] = p.Location
	fields["Face"] = p.Face
	fields["Sequence"] = p.Sequence
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *BlockDig) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Status"]; ok {
		p.Status = val.(pk.VarInt)
	}
	if val, ok := fields["Location"]; ok {
		p.Location = val.(basetypes.Position)
	}
	if val, ok := fields["Face"]; ok {
		p.Face = val.(pk.Byte)
	}
	if val, ok := fields["Sequence"]; ok {
		p.Sequence = val.(pk.VarInt)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetStatus returns the Status field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *BlockDig) GetStatus() pk.VarInt {
	return p.Status
}

// SetStatus sets the Status field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *BlockDig) SetStatus(val pk.VarInt) {
	p.Status = val
}

// GetLocation returns the Location field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *BlockDig) GetLocation() basetypes.Position {
	return p.Location
}

// SetLocation sets the Location field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *BlockDig) SetLocation(val basetypes.Position) {
	p.Location = val
}

// GetFace returns the Face field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *BlockDig) GetFace() pk.Byte {
	return p.Face
}

// SetFace sets the Face field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *BlockDig) SetFace(val pk.Byte) {
	p.Face = val
}

// GetSequence returns the Sequence field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *BlockDig) GetSequence() pk.VarInt {
	return p.Sequence
}

// SetSequence sets the Sequence field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *BlockDig) SetSequence(val pk.VarInt) {
	p.Sequence = val
}

func (t *BlockDig) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Status.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Status")
	}
	bytesRead, err = t.Location.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Location")
	}
	bytesRead, err = t.Face.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Face")
	}
	bytesRead, err = t.Sequence.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Sequence")
	}

	return totalBytes, nil
}

func (t BlockDig) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[BlockDig.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Status.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Location.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Face.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Sequence.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
