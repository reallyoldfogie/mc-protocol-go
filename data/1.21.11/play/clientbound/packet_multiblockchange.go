// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "chunkCoordinates",
//	      "type": [
//	        "bitfield",
//	        [
//	          {
//	            "name": "x",
//	            "size": 22,
//	            "signed": true
//	          },
//	          {
//	            "name": "z",
//	            "size": 22,
//	            "signed": true
//	          },
//	          {
//	            "name": "y",
//	            "size": 20,
//	            "signed": true
//	          }
//	        ]
//	      ]
//	    },
//	    {
//	      "name": "records",
//	      "type": [
//	        "array",
//	        {
//	          "countType": "varint",
//	          "type": "varint"
//	        }
//	      ]
//	    }
//	  ]
//	]
type MultiBlockChange struct {
	packetID int32
	// [
	//                 "bitfield",
	//                 [
	//                   {
	//                     "name": "x",
	//                     "size": 22,
	//                     "signed": true
	//                   },
	//                   {
	//                     "name": "z",
	//                     "size": 22,
	//                     "signed": true
	//                   },
	//                   {
	//                     "name": "y",
	//                     "size": 20,
	//                     "signed": true
	//                   }
	//                 ]
	//               ]
	ChunkCoordinates MultiBlockChangeChunkCoordinates
	// [
	//                 "array",
	//                 {
	//                   "countType": "varint",
	//                   "type": "varint"
	//                 }
	//               ]
	Records models.Array[pk.VarInt, pk.VarInt]
}

// NewMultiBlockChange creates a new MultiBlockChange packet with the correct packet ID.
func NewMultiBlockChange() *MultiBlockChange {
	return &MultiBlockChange{packetID: 82}
}

// PacketID returns the protocol ID for this packet type.
func (p *MultiBlockChange) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *MultiBlockChange) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *MultiBlockChange) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.ChunkCoordinates,
		&p.Records)
}

// Scan deserializes a wire-format packet into this struct.
func (p *MultiBlockChange) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.ChunkCoordinates,
		&p.Records)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *MultiBlockChange) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["ChunkCoordinates"] = &p.ChunkCoordinates
	fields["Records"] = &p.Records
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *MultiBlockChange) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["ChunkCoordinates"]; ok {
		p.ChunkCoordinates = *val.(*MultiBlockChangeChunkCoordinates)
	}
	if val, ok := fields["Records"]; ok {
		p.Records = *val.(*models.Array[pk.VarInt, pk.VarInt])
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetChunkCoordinates returns the ChunkCoordinates field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *MultiBlockChange) GetChunkCoordinates() MultiBlockChangeChunkCoordinates {
	return p.ChunkCoordinates
}

// SetChunkCoordinates sets the ChunkCoordinates field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *MultiBlockChange) SetChunkCoordinates(val MultiBlockChangeChunkCoordinates) {
	p.ChunkCoordinates = val
}

// GetRecords returns the Records field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *MultiBlockChange) GetRecords() models.Array[pk.VarInt, pk.VarInt] {
	return p.Records
}

// SetRecords sets the Records field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *MultiBlockChange) SetRecords(val models.Array[pk.VarInt, pk.VarInt]) {
	p.Records = val
}

func (t *MultiBlockChange) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.ChunkCoordinates.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field ChunkCoordinates")
	}
	bytesRead, err = t.Records.ReadFrom(r)

	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Records")
	}

	return totalBytes, nil
}

func (t MultiBlockChange) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[MultiBlockChange.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.ChunkCoordinates.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Records.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}

type MultiBlockChangeChunkCoordinates struct {
	X int64
	Z int64
	Y int64
}

func (b *MultiBlockChangeChunkCoordinates) ReadFrom(r io.Reader) (int64, error) {
	// Calculate total bits and bytes needed
	totalBits := 0
	totalBits += 22
	totalBits += 22
	totalBits += 20

	if totalBits%8 != 0 {
		return 0, fmt.Errorf("bitfield MultiBlockChangeChunkCoordinates total size %d is not a multiple of 8", totalBits)
	}

	numBytes := totalBits / 8
	data := make([]byte, numBytes)

	nn, err := io.ReadFull(r, data)
	if err != nil {
		return int64(nn), errors.Wrap(err, "failed to read bitfield MultiBlockChangeChunkCoordinates")
	}

	// Convert bytes to uint64 (big-endian)
	var packed uint64
	for i := 0; i < numBytes; i++ {
		packed |= uint64(data[i]) << (8 * (numBytes - 1 - i))
	}

	// Extract bit fields
	currentOffset := 0
	// Extract x (22 bits, signed=true)
	x_mask := uint64((1 << 22) - 1)
	x_value := (packed >> (totalBits - currentOffset - 22)) & x_mask
	// Sign extend if negative
	if x_value&(1<<(22-1)) != 0 {
		// Sign extend by converting to signed and back
		signBit := uint64(1) << 22
		x_value = x_value - signBit
	}
	b.X = int64(x_value)
	currentOffset += 22
	// Extract z (22 bits, signed=true)
	z_mask := uint64((1 << 22) - 1)
	z_value := (packed >> (totalBits - currentOffset - 22)) & z_mask
	// Sign extend if negative
	if z_value&(1<<(22-1)) != 0 {
		// Sign extend by converting to signed and back
		signBit := uint64(1) << 22
		z_value = z_value - signBit
	}
	b.Z = int64(z_value)
	currentOffset += 22
	// Extract y (20 bits, signed=true)
	y_mask := uint64((1 << 20) - 1)
	y_value := (packed >> (totalBits - currentOffset - 20)) & y_mask
	// Sign extend if negative
	if y_value&(1<<(20-1)) != 0 {
		// Sign extend by converting to signed and back
		signBit := uint64(1) << 20
		y_value = y_value - signBit
	}
	b.Y = int64(y_value)
	currentOffset += 20

	return int64(nn), nil
}

func (b MultiBlockChangeChunkCoordinates) WriteTo(w io.Writer) (int64, error) {
	// Calculate total bits and bytes needed
	totalBits := 0
	totalBits += 22
	totalBits += 22
	totalBits += 20

	if totalBits%8 != 0 {
		return 0, fmt.Errorf("bitfield MultiBlockChangeChunkCoordinates total size %d is not a multiple of 8", totalBits)
	}

	numBytes := totalBits / 8

	// Pack bit fields into uint64
	var packed uint64
	currentOffset := 0
	// Pack x (22 bits)
	x_value := uint64(b.X) & ((1 << 22) - 1)
	packed |= x_value << (totalBits - currentOffset - 22)
	currentOffset += 22
	// Pack z (22 bits)
	z_value := uint64(b.Z) & ((1 << 22) - 1)
	packed |= z_value << (totalBits - currentOffset - 22)
	currentOffset += 22
	// Pack y (20 bits)
	y_value := uint64(b.Y) & ((1 << 20) - 1)
	packed |= y_value << (totalBits - currentOffset - 20)
	currentOffset += 20

	// Convert uint64 to bytes (big-endian)
	data := make([]byte, numBytes)
	for i := 0; i < numBytes; i++ {
		data[i] = byte(packed >> (8 * (numBytes - 1 - i)))
	}

	nn, err := w.Write(data)
	return int64(nn), err
}
