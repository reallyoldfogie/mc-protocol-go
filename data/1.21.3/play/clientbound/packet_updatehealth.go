// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "health",
//	      "type": "f32"
//	    },
//	    {
//	      "name": "food",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "foodSaturation",
//	      "type": "f32"
//	    }
//	  ]
//	]
type UpdateHealth struct {
	packetID int32
	// "f32"
	Health pk.Float
	// "varint"
	Food pk.VarInt
	// "f32"
	FoodSaturation pk.Float
}

// NewUpdateHealth creates a new UpdateHealth packet with the correct packet ID.
func NewUpdateHealth() *UpdateHealth {
	return &UpdateHealth{packetID: 98}
}

// PacketID returns the protocol ID for this packet type.
func (p *UpdateHealth) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *UpdateHealth) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *UpdateHealth) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Health,
		&p.Food,
		&p.FoodSaturation)
}

// Scan deserializes a wire-format packet into this struct.
func (p *UpdateHealth) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Health,
		&p.Food,
		&p.FoodSaturation)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *UpdateHealth) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Health"] = p.Health
	fields["Food"] = p.Food
	fields["FoodSaturation"] = p.FoodSaturation
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *UpdateHealth) SetFields(fields map[string]pk.FieldEncoder) {
	fmt.Printf("<no value>\n")
	if val, ok := fields["Health"]; ok {
		p.Health = val.(pk.Float)
	}
	if val, ok := fields["Food"]; ok {
		p.Food = val.(pk.VarInt)
	}
	if val, ok := fields["FoodSaturation"]; ok {
		p.FoodSaturation = val.(pk.Float)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetHealth returns the Health field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateHealth) GetHealth() pk.Float {
	return p.Health
}

// SetHealth sets the Health field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateHealth) SetHealth(val pk.Float) {
	p.Health = val
}

// GetFood returns the Food field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateHealth) GetFood() pk.VarInt {
	return p.Food
}

// SetFood sets the Food field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateHealth) SetFood(val pk.VarInt) {
	p.Food = val
}

// GetFoodSaturation returns the FoodSaturation field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *UpdateHealth) GetFoodSaturation() pk.Float {
	return p.FoodSaturation
}

// SetFoodSaturation sets the FoodSaturation field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *UpdateHealth) SetFoodSaturation(val pk.Float) {
	p.FoodSaturation = val
}

func (t *UpdateHealth) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Health.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Health")
	}
	bytesRead, err = t.Food.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Food")
	}
	bytesRead, err = t.FoodSaturation.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field FoodSaturation")
	}

	return totalBytes, nil
}

func (t UpdateHealth) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[UpdateHealth.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Health.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.Food.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.FoodSaturation.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
