// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"bytes"
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"github.com/reallyoldfogie/mc-protocol-go/models"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "id",
//	      "type": "varint"
//	    },
//	    {
//	      "name": "signature",
//	      "type": [
//	        "switch",
//	        {
//	          "compareTo": "id",
//	          "fields": {
//	            "0": [
//	              "buffer",
//	              {
//	                "count": 256
//	              }
//	            ]
//	          },
//	          "default": "void"
//	        }
//	      ]
//	    }
//	  ]
//	]
type HideMessage struct {
	packetID int32
	// "varint"
	Id pk.VarInt
	// [
	//                 "switch",
	//                 {
	//                   "compareTo": "id",
	//                   "fields": {
	//                     "0": [
	//                       "buffer",
	//                       {
	//                         "count": 256
	//                       }
	//                     ]
	//                   },
	//                   "default": "void"
	//                 }
	//               ]
	Signature pk.Field
}

// NewHideMessage creates a new HideMessage packet with the correct packet ID.
func NewHideMessage() *HideMessage {
	return &HideMessage{packetID: 28}
}

// PacketID returns the protocol ID for this packet type.
func (p *HideMessage) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *HideMessage) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *HideMessage) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Id,
		p.Signature)
}

// Scan deserializes a wire-format packet into this struct.
func (p *HideMessage) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	// This packet has switch fields, so we read fields individually
	r := bytes.NewReader(packet.Data)
	var totalBytes int64
	var bytesRead int64
	var err error
	bytesRead, err = p.Id.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return fmt.Errorf("scanning packet field[Id] error: %w", err)
	}
	// Switch field Signature based on id
	// Convert compareTo value to string for matching
	compareValueSignature := fmt.Sprintf("%v", p.Id)

	switch compareValueSignature {
	case "0":
		var val models.FixedBuffer256
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "scanning packet field[Signature] case 0")
		}
		p.Signature = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return errors.Wrap(err, "failed to read void switch field Signature default case")
		}
		p.Signature = &__void
	}

	_ = totalBytes // Unused in Scan()
	return nil
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *HideMessage) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Id"] = &p.Id
	fields["Signature"] = p.Signature
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *HideMessage) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Id"]; ok {
		p.Id = *val.(*pk.VarInt)
	}
	if val, ok := fields["Signature"]; ok {
		p.Signature = val.(pk.Field)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetId returns the Id field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *HideMessage) GetId() pk.VarInt {
	return p.Id
}

// SetId sets the Id field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *HideMessage) SetId(val pk.VarInt) {
	p.Id = val
}

// GetSignature returns the Signature field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *HideMessage) GetSignature() pk.Field {
	return p.Signature
}

// SetSignature sets the Signature field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *HideMessage) SetSignature(val pk.Field) {
	p.Signature = val
}

func (t *HideMessage) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Id.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Id")
	}
	// Switch field Signature based on id
	// Convert compareTo value to string for matching
	compareValueSignature := fmt.Sprintf("%v", t.Id)

	switch compareValueSignature {
	case "0":
		var val models.FixedBuffer256
		bytesRead, err = val.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read switch field Signature case 0")
		}
		t.Signature = &val
	default:
		// Void case - no data to read
		var __void models.Void
		bytesRead, err = __void.ReadFrom(r)
		totalBytes += bytesRead
		if err != nil {
			return totalBytes, errors.Wrap(err, "failed to read void switch field Signature default case")
		}
		t.Signature = &__void
	}

	return totalBytes, nil
}

func (t HideMessage) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[HideMessage.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Id.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	// Switch field Signature based on id
	if t.Signature != nil {
		// Write switch field value if it implements WriteTo
		if writer, ok := t.Signature.(interface {
			WriteTo(io.Writer) (int64, error)
		}); ok {
			bytesWritten, err = writer.WriteTo(w)
			totalBytes += bytesWritten
			if err != nil {
				return totalBytes, err
			}
		} else {
			// Not a void case and doesn't implement WriteTo
			return totalBytes, fmt.Errorf("switch field Signature value does not implement WriteTo: %T", t.Signature)
		}
	}
	return totalBytes, nil
}
