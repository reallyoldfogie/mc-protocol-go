// Generated by gen_protocol. DO NOT EDIT
package clientbound

import (
	"fmt"
	pk "github.com/Tnze/go-mc/net/packet"
	"github.com/pkg/errors"
	"io"
	"log"
)

// Protodef: [
//
//	  "container",
//	  [
//	    {
//	      "name": "difficulty",
//	      "type": "u8"
//	    },
//	    {
//	      "name": "difficultyLocked",
//	      "type": "bool"
//	    }
//	  ]
//	]
type Difficulty struct {
	packetID int32
	// "u8"
	Difficulty pk.UnsignedByte
	// "bool"
	DifficultyLocked pk.Boolean
}

// NewDifficulty creates a new Difficulty packet with the correct packet ID.
func NewDifficulty() *Difficulty {
	return &Difficulty{packetID: 11}
}

// PacketID returns the protocol ID for this packet type.
func (p *Difficulty) PacketID() int32 {
	return p.packetID
}

// SetPacketID sets the protocol ID for this packet type.
// This is used when the same packet structure is reused across multiple stages with different IDs.
func (p *Difficulty) SetPacketID(id int32) {
	p.packetID = id
}

// Marshal serializes the packet into wire format.
func (p *Difficulty) Marshal() pk.Packet {
	return pk.Marshal(
		p.packetID,
		&p.Difficulty,
		&p.DifficultyLocked)
}

// Scan deserializes a wire-format packet into this struct.
func (p *Difficulty) Scan(packet pk.Packet) error {
	if packet.ID != p.packetID {
		return fmt.Errorf("packet ID mismatch: expected %d, got %d", p.packetID, packet.ID)
	}
	return packet.Scan(
		&p.Difficulty,
		&p.DifficultyLocked)
}

// GetFields returns a map of all packet fields for version-agnostic access.
// Use this when you need to access fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed getter methods (e.g., GetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountGetter).
func (p *Difficulty) GetFields() map[string]pk.FieldEncoder {
	fields := map[string]pk.FieldEncoder{}
	fields["Difficulty"] = &p.Difficulty
	fields["DifficultyLocked"] = &p.DifficultyLocked
	return fields
}

// SetFields updates packet fields from a map for version-agnostic access.
// Use this when you need to set fields dynamically or when working with version-specific types
// that don't have stable cross-version interfaces.
//
// For version-specific code with type safety, use the typed setter methods (e.g., SetCount()).
// For semi-agnostic code with fields that have stable types, use the typed interfaces (e.g., CountSetter).
func (p *Difficulty) SetFields(fields map[string]pk.FieldEncoder) {
	if val, ok := fields["Difficulty"]; ok {
		p.Difficulty = *val.(*pk.UnsignedByte)
	}
	if val, ok := fields["DifficultyLocked"]; ok {
		p.DifficultyLocked = *val.(*pk.Boolean)
	}
}

// Typed field accessor methods for version-specific type-safe access
// GetDifficulty returns the Difficulty field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Difficulty) GetDifficulty() pk.UnsignedByte {
	return p.Difficulty
}

// SetDifficulty sets the Difficulty field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Difficulty) SetDifficulty(val pk.UnsignedByte) {
	p.Difficulty = val
}

// GetDifficultyLocked returns the DifficultyLocked field value.
// Note: This method returns the actual field type, which may be version-specific.
// For version-agnostic access, use GetFields() or check for typed interfaces.
func (p *Difficulty) GetDifficultyLocked() pk.Boolean {
	return p.DifficultyLocked
}

// SetDifficultyLocked sets the DifficultyLocked field value.
// Note: This method accepts the actual field type, which may be version-specific.
// For version-agnostic access, use SetFields() or check for typed interfaces.
func (p *Difficulty) SetDifficultyLocked(val pk.Boolean) {
	p.DifficultyLocked = val
}

func (t *Difficulty) ReadFrom(r io.Reader) (totalBytes int64, err error) {
	var bytesRead int64
	bytesRead, err = t.Difficulty.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field Difficulty")
	}
	bytesRead, err = t.DifficultyLocked.ReadFrom(r)
	totalBytes += bytesRead
	if err != nil {
		return totalBytes, errors.Wrap(err, "failed to read field DifficultyLocked")
	}

	return totalBytes, nil
}

func (t Difficulty) WriteTo(w io.Writer) (totalBytes int64, err error) {
	var bytesWritten int64

	defer func() {
		log.Printf("[Difficulty.WriteTo] totalBytes: %d err: %#v", totalBytes, err)
	}()
	bytesWritten, err = t.Difficulty.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	bytesWritten, err = t.DifficultyLocked.WriteTo(w)
	totalBytes += bytesWritten
	if err != nil {
		return totalBytes, err
	}
	return totalBytes, nil
}
