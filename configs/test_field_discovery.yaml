# Test Configuration for Field Discovery
versions:
  - "1.21.6"
  - "1.21.1"

output:
  data_dir: "data"

cache:
  cache_dir: ".cache"
  metadata_dir: ".cache/metadata"
  ttl_days: 7

memory:
  gc_percent: 50
  memory_limit: "1GiB"
  max_procs: 4
